app:
  test-mode: true

  state:
    code: CA

  datamart:
    datasource:
      url-schema: reporting_test
      initialSize: 4
      maxActive: 10
      maxIdle: 10
      maxWait: 10000

  iris:
    url: http://my-iris.somewhere.org/iris/

permissionservice:
  endpoint:
artifacts:
  root: "classpath:"

permissionservice:
  endpoint:

app:
  state:
    code: CA

  datamart:
    datasource:
      url: jdbc:mysql://localhost:3306/reporting_test?useSSL=false&useLegacyDatetimeCode=false

  iris:
    vendorId: vendorId
    url: http://my-iris.somewhere.org/iris/

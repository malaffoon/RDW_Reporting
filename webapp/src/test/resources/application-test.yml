spring:
  datasource:
    url: jdbc:mysql://localhost:3306/warehouse_test?useSSL=false&useLegacyDatetimeCode=false&characterEncoding=utf8

artifacts:
  root: "classpath:"

permissionservice:
  endpoint:

application:
  state: CA

app:
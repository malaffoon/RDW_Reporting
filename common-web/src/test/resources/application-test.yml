reporting:
  state:
    code: CA

spring:
  reporting_ro_datasource:
    url: jdbc:mysql://${spring.reporting_ro_datasource.url-server:localhost:3306}/${spring.reporting_ro_datasource.url-schema:reporting_test}?useSSL=false&useLegacyDatetimeCode=false&characterEncoding=utf8
    username: root
    password:
    driverClassName: com.mysql.jdbc.Driver

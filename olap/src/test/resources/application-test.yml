app:
  datamart:
     olap_datasource:
        url: "\
            jdbc:redshift://${app.datamart.olap_datasource.url-server:localhost:5432}/dev\
            ?ApplicationName=rdw-reporting-olap\
            &currentSchema=${app.datamart.olap_datasource.url-schema:reporting_olap_test}
           "
        username: awstest
        password: Password4U
        driverClassName: com.amazon.redshift.jdbc42.Driver

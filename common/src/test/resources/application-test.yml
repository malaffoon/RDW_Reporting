reporting:
  state:
    code: CA
  transfer-access-enabled: false

spring:
  reporting_ro_datasource:
    url-parts:
      #This is the generic format of the connection URL protocol//[hosts][/database][?properties]
      protocol: "jdbc:mysql:"
      hosts: "${spring.reporting_ro_reporting_ro_datasource.url-server:localhost:3306}"
      database: "${spring.reporting_ro_reporting_ro_datasource.url-schema:reporting_test}"
      properties: "useSSL=false&useLegacyDatetimeCode=false&characterEncoding=utf8"
    url: "${spring.reporting_ro_datasource.url-parts.protocol}//${spring.reporting_ro_datasource.url-parts.hosts}/${spring.reporting_ro_datasource.url-parts.database}?${spring.reporting_ro_datasource.url-parts.properties}"
    username: root
    password:
    driverClassName: com.mysql.jdbc.Driver

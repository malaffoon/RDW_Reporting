spring:
  reporting_datasource:
    url-schema: reporting_test
  warehouse_datasource:
    url-schema: warehouse_test

artifacts:
  root: "classpath:"

app:
  test-mode: true

reporting:
  school-year: 2018
  state:
    code: CA
    name: California

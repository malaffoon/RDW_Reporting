logging:
  level:
    # emit statistics for the underlying context cache
    org.opentestsystem.rdw.admin: debug

tenant-configuration-persistence:
  local-repository-path: /tmp/rdw_config
  #remote-repository-uri:
  git-username: fairwaytech_rdw
  git-password: ALIteGingeOG
  author: "Zaphod Beeblebrox"
  author-email: "zaphodb@example.com"